# Convenience aliases

alias ll "ls -al"
alias tt "tree -L 2"  # Get overview of current directory
alias gs "git status"
alias jj "just --unstable"

alias cdr "cd (git rev-parse --show-toplevel)"

alias vim "nvim"
alias vi "/usr/bin/vim"
